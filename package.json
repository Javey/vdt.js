{
  "name": "vdt.js",
  "version": "0.0.1",
  "description": "A template engine using virtual-dom",
  "dependencies": {
    "virtual-dom": "^2.0.1"
  },
  "scripts": {
    "build": "browserify src/vdt.js -o build/vdt.js -s Vdt && uglifyjs build/vdt.js -o build/vdt.min.js -m"
  }
}
